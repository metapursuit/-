# Errors — 命令失败/工具坑捕获

> **定位**：工具/命令/环境类失败事件。修复后状态改 resolved；复发用 Pattern-Key 折叠（bump Recurrence-Count）。
> **格式**：追加不修改；Pattern-Key 去重（area.symptom）。
> **时间戳**：`Logged: YYYY-MM-DDTHH:MM`（Asia/Shanghai）——写条目前先 `date` 校准。

---

（空——暂无未解决错误。历史坑位见 MEMORY 各条目与 git-bash-windows-paths skill。）
